export class PolygonData {
    data: number[];
    max: number;
    // getMax(): number {
    //     if (undefined === this.max) {
    //         for (let i = 0; this.data && i < this.data.length; i++) {
    //
    //         }
    //     }
    // }
}